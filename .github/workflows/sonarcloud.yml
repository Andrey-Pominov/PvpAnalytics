name: SonarCloud Analysis

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build and analyze
    runs-on: windows-latest
    steps:
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'zulu'

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ui/PvpAnalytics.UI/package-lock.json

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Node.js dependencies
        working-directory: ui/PvpAnalytics.UI
        run: npm ci

      - name: Build TypeScript project
        working-directory: ui/PvpAnalytics.UI
        run: npm run build

      - name: Cache SonarQube Cloud packages
        uses: actions/cache@v4
        with:
          path: ~\sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache SonarQube Cloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner

      - name: Install SonarQube Cloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path ${{ runner.temp }}\scanner -ItemType Directory -Force
          dotnet tool update dotnet-sonarscanner --tool-path ${{ runner.temp }}\scanner

      - name: Begin SonarQube analysis
        shell: powershell
        env:
          SONAR_TOKEN: ${{ 0c16f5b28e94de21763276bb11ba349b38deb4e3 }}
        run: |
          ${{ runner.temp }}\scanner\dotnet-sonarscanner begin `
            /k:"Andrey-Pominov_PvpAnalytics" `
            /o:"andrey-pominov" `
            /d:sonar.token="${{ 0c16f5b28e94de21763276bb11ba349b38deb4e3 }}" `
            /d:sonar.cs.opencover.reportsPaths="**/coverage.opencover.xml" `
            /d:sonar.javascript.lcov.reportPaths="ui/PvpAnalytics.UI/coverage/lcov.info" `
            /d:sonar.sources="Services,Shared,ui/PvpAnalytics.UI/src" `
            /d:sonar.exclusions="**/bin/**,**/obj/**,**/node_modules/**,**/dist/**,**/*.min.js,**/Migrations/**" `
            /d:sonar.sourceEncoding=UTF-8 `
            /d:sonar.csharp.file.suffixes=.cs `
            /d:sonar.typescript.file.suffixes=.ts,.tsx `
            /d:sonar.javascript.file.suffixes=.js,.jsx

      - name: Build .NET solution
        run: dotnet build PvpAnalytics.sln --configuration Release --no-incremental

      - name: Run .NET tests (if any)
        continue-on-error: true
        run: dotnet test PvpAnalytics.sln --configuration Release --no-build --verbosity normal --collect:"XPlat Code Coverage" --results-directory ./coverage

      - name: End SonarQube analysis
        shell: powershell
        env:
          SONAR_TOKEN: ${{ 0c16f5b28e94de21763276bb11ba349b38deb4e3 }}
        run: |
          ${{ runner.temp }}\scanner\dotnet-sonarscanner end /d:sonar.token="${{ 0c16f5b28e94de21763276bb11ba349b38deb4e3 }}"
